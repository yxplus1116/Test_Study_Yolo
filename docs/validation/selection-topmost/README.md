# 目标标注与置顶验证

2026-09-12。目标选择算法、模型与鼠标输出条件未更改。

- `build.log`：Release 编译、部署成功，CTest 核心回归通过。新回归覆盖低置信度但靠中心的目标胜出、已有目标优先继续 IoU 关联、当帧所选框元数据失配清空及随后恢复、HEAD/BODY 分数不会混入所选目标说明、暂停清除黄框和红圈。
- `sample-frames.csv`：仓库 `zgjr.jpg` 在头部模式下实际推理得到两个头部候选。已查看新版预览：选中框为黄色、标为 `SELECTED HEAD C1 67%`；另一框为绿色、标为 `HEAD C1 68%`。较高分并不会自动覆盖首次距离排序。此样例只验证显示与选择，不代表模型在自然照片上的定位准确率。
- `window.json`：重新启动原菜单管理的头部预览后，实际读取 HWND 扩展样式，确认 `WS_EX_TOPMOST` 已设置且窗口可见。恢复了更新前窗口的位置；本次预览未启用鼠标输出。
- 置顶使用一次 `SetWindowPos(HWND_TOPMOST, ... SWP_NOACTIVATE)`，没有增加逐帧抢焦点逻辑。

框颜色改为：黄色为选中目标，绿色为当前类别其他候选，灰色为其他类别；文字优先为选中目标安排位置并避开已有标签。固定顶部始终说明当前有效选中目标自身的类别与百分比；没有空间的框旁文字会略去，避免显示被截断的分数。无新帧、暂停或采集失败时不会沿用旧的选择高亮。
