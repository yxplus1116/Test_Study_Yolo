# 输入失效诊断更新验证

2026-09-12。

- Release 编译和核心回归通过，详见 `build.log`。
- 68 项菜单路由/解析/生命周期记录检查通过。菜单 11 新增 6“鼠标失效排查说明”。
- 离线图片 20 帧检查通过：产生控制计算回调但 attempted/injected 均为 0；上下文日志存在，发送方权限可读取，逐帧门禁和测量年龄字段存在。
- 实时 GDI 无预览 3 秒检查通过：266 帧、3 个上下文样本、0 次输入尝试。此次进程为 medium、未提升；不能据此推断用户其他会话或未知目标应用的权限。
- 原运行测试通过，覆盖无效输入、离线输出拒绝、干运行、两种采集和运行/构建取消，详见 `runtime-smoke.json`。
- 只查询前台进程身份/权限和光标状态，未启用真实输入测试、未修改目标应用、未模拟失败事件、未改变发送后端或目标选择算法。

本次验证范围是新增诊断的落盘与现有行为回归，尚未复现用户指定应用里的输入不响应；目标程序名称仍未知。具体排查结论及边界见 [输入失效说明](D:/Projects/Test_Study_Yolo/docs/input-troubleshooting.md)。
